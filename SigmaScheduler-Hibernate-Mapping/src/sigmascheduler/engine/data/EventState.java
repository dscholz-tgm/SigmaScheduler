package sigmascheduler.engine.data;

/**
 * Enum EventState (Diese Klasse wird nicht gemapped, wird aber trotzdem
 * benoetigt)
 * 
 * @author Kodras, Oezsoy, Vogt
 */
public enum EventState {

	UNPUBLISHED, VOTEABLE, CLOSED, OVER;

}